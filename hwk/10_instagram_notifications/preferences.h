#ifndef USER_H
#define USER_H

#include <string>

class Preferences {
public:
    bool pauseAll;
    bool likes;
    bool tags;
    bool comments;
    bool newFollowers;
    bool messageRequests;
};

#endif